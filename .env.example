# VITE_GEMINI_API_KEY: Required for Agent Herald cognition (Gemini-powered agent).
# Must use the VITE_ prefix so Vite exposes it to the browser via import.meta.env.
# Without this key the agent will report "VITE_GEMINI_API_KEY is missing" and cognition will be disabled.
# Get a key at: https://aistudio.google.com/app/apikey
VITE_GEMINI_API_KEY="AIza..."

# APP_URL: The URL where this app is hosted.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="http://localhost:3000"
